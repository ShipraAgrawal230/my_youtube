structure:
-head
-body
    -sidebar
        -menuitems
    -mainContainer
        -buttonslist
        -video -mainContainer  
            -video card

-debouncing is the skipping the events. ex-search in the searchbar
-if the cache size is >100 start removing from top
-problems in live streaming:
    -get live data
        -websockets(bidirectional: two way handshaking b/w client and server)
        -Api polling(unidirectly: data flows from server to UI)
            -ex- in case of gmail we can use polling bcz there is no need of instant data updation we can do it even after 10s 0r 20s.
            -ex- but in case of trading apps or whatsapp or live commentary cricket apps we should use websockets.
            -ex- utube uses api polling.
    -update on UI

-useMemo() hook lets u cache the results of a calculation between re-renders.
even though when i change the theme it still calculate the prime value everytime bczon toggling the component will re-render everytime.

-useCallback() hook lets u cache a function definition between re-renders.